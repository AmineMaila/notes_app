{
  "name": "backend",
  "version": "0.0.1",
  "description": "backend testing",
  "main": "index.js",
  "scripts": {
		"build:ui": "rm -rf dest && cd ../frontend/ && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
		"start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amine maila",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.3"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.0"
  }
}
